---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "conductorone_vault Resource - terraform-provider-conductorone"
subcategory: ""
description: |-
  Vault Resource
---

# conductorone_vault (Resource)

Vault Resource

## Example Usage

```terraform
resource "conductorone_vault" "my_vault" {
  description  = "...my_description..."
  display_name = "...my_display_name..."
  group_authz_vault = {
    # ...
  }
  magic_vault = {
    allow_unauthed_views = false
    allowed_views        = 5
  }
  owner_ids = [
    "..."
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `display_name` (String) The human-readable name for the new vault.

### Optional

- `description` (String) A free-text description of the vault's purpose or configuration.
- `group_authz_vault` (Attributes) GroupAuthzVault configures a vault that uses group-based authorization to control access to stored credentials. (see [below for nested schema](#nestedatt--group_authz_vault))
- `magic_vault` (Attributes) MagicVault configures a vault that grants time-limited credential access via magic links. (see [below for nested schema](#nestedatt--magic_vault))
- `owner_ids` (List of String) The IDs of users to assign as owners of this vault. Requires replacement if changed.

### Read-Only

- `created_at` (String)
- `credential_expiration_duration` (String)
- `id` (String) The unique identifier of the vault.
- `updated_at` (String)

<a id="nestedatt--group_authz_vault"></a>
### Nested Schema for `group_authz_vault`


<a id="nestedatt--magic_vault"></a>
### Nested Schema for `magic_vault`

Optional:

- `allow_unauthed_views` (Boolean) Controls whether unauthenticated users can view credentials via a magic link.
- `allowed_views` (Number) The maximum number of times a credential in this vault may be viewed.

## Import

Import is supported using the following syntax:

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = conductorone_vault.my_conductorone_vault
  id = "..."
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import conductorone_vault.my_conductorone_vault "..."
```
